/**
 * Página delgada para Expo Router
 * Solo orquesta el componente contenedor (screen)
 */

import { BranchesListScreen } from '@/src/features/security/branches/screens/branches-list.screen';

export default function BranchesListPage() {
  return <BranchesListScreen />;
}
