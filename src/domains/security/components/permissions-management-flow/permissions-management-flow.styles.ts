/**
 * Estilos del flujo de administración de permisos.
 * Reutiliza la misma factory que role-permissions-flow.
 */

export { createPermissionFlowStyles } from "../role-permissions-flow/role-permissions-flow.styles";
