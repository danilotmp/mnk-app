/**
 * Estilos del componente DynamicIcon.
 * El componente recibe style por props; este archivo cumple el patrón de arquitectura.
 */

import { StyleSheet } from "react-native";

export const dynamicIconStyles = StyleSheet.create({
  // Sin estilos por defecto; el icono usa style prop
});
