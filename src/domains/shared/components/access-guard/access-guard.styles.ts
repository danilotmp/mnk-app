/**
 * Estilos del componente AccessGuard (fallback de acceso denegado)
 */

import { StyleSheet } from "react-native";

export const accessGuardStyles = StyleSheet.create({
  deniedContainer: {
    padding: 20,
    alignItems: "center",
  },
  deniedMessage: {
    marginTop: 8,
  },
});
